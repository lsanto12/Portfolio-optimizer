# Portfolio-optimizatior
Portfolio weight optimization over 19 funds: correlated GBM Monte Carlo with variance reduction, and a softmax neural optimizer with a modular loss (Sharpe, CVaR, entropy) whose gradient steps are scaled by a PID controller. PyTorch/CUDA
